<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.enterprise.demo.sys.dao.RoleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.enterprise.demo.sys.entity.Role">
        <id column="id" property="id"/>
        <result column="role_id" property="roleId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="gmt_create" property="gmtCreate"/>
        <result column="gmt_modified" property="gmtModified"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, role_id AS roleId, name, description, status, gmt_create AS gmtCreate, gmt_modified AS gmtModified
    </sql>

    <select id="findRoleByUserId" resultType="java.lang.String">
        SELECT r.role_id FROM role r LEFT JOIN user_role ur ON r.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

</mapper>
